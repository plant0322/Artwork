<% @rooms_user.each do |user| %>
  <% if current_user.following?(user) %>
    <div>
      <%= link_to chat_path(user) do %>
        <%= image_tag user.get_profile_image(70,70) %>
        <%= user.name %>とのDM
      <% end %>
    </div>
  <% end %>
<% end %>