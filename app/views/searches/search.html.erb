<div class="conteiner px-5">
  <div class="row">
    <div class="col-md-6 mx-auto">
      <% if @model == 'user' %>
        <p>"<%= @content %>"でユーザーを検索した結果…</p>
        <%= render 'users/index', users: @records %>
      <% else %>
        <p>"<%= @content %>"で投稿を検索した結果…</p>
        <%= render 'works/index', works: @records %>
      <% end %>
    </div>
  </div>
</div>